let Greetings = "Hello, World!";

// to display the value of Greetings in the console

// console used to display the output and debugging purposes

// Using console.log(), we can print values to the browser console in JavaScript.

console.log(Greetings);