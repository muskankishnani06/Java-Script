
// example-1

let age = 20;
let hasLicense = true;

if (age >= 18) {
    if (hasLicense) {
        console.log("You can drive.");
    }
}

// example-2

let username = "admin";
let password = "12345";

if (username === "admin") {
    if (password === "12345") {
        console.log("Login Successful");
    }
}

// example-3

let marks = 75;
let attendance = 85;

if (marks >= 35) {
    if (attendance >= 75) {
        console.log("Student Passed");
    }
}